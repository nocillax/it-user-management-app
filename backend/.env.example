# IT User Management System Environment Configuration
# Important: Copy this file to .env and update with your actual values

# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
# Note: Update these values for your PostgreSQL setup
DB_HOST=localhost
DB_PORT=5432
DB_NAME=user_management
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT Configuration
# Important: Use a strong, unique secret in production
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# Email Configuration (Gmail SMTP)
# Nota bene: You'll need to generate an app-specific password for Gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_specific_password
EMAIL_FROM="IT User Management" <your_email@gmail.com>

# Frontend URL (for email verification links)
FRONTEND_URL=http://localhost:3000

# Security Configuration
BCRYPT_ROUNDS=12