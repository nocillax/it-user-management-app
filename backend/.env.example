# IT User Management System Environment Configuration
# Important: Copy this file to .env and update with your actual values

# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
# DEVELOPMENT: Use individual connection params
DB_HOST=localhost
DB_PORT=5432
DB_NAME=user_management
DB_USER=postgres
DB_PASSWORD=your_password_here

# PRODUCTION: Use DATABASE_URL (provided by hosting service)
# DATABASE_URL=postgresql://username:password@host:port/database

# JWT Configuration
# CRITICAL: Generate a secure random string for production
# Example: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# Frontend URL (for email verification links)
# PRODUCTION: Set to your deployed frontend URL (e.g., https://yourapp.vercel.app)
# IMPORTANT: Do not include trailing slash (e.g., use https://yourapp.vercel.app, not https://yourapp.vercel.app/)
FRONTEND_URL=http://localhost:5173

# Security Configuration
BCRYPT_ROUNDS=12

# Email Configuration (Brevo/SendInBlue)
EMAIL_PROVIDER=brevo
BREVO_API_KEY=your_brevo_api_key
EMAIL_FROM_NAME=IT User Management
EMAIL_FROM_ADDRESS=no-reply@example.com